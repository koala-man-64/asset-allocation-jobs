# Transitional decomposition surface; implementation remains in the stable top-level finance job module.
from tasks.finance_data.bronze_finance_data import (
    _ThreadLocalMassiveClientManager,
    _decode_payload_json,
    _emit_bounded_trace,
    _extract_latest_finance_report_date,
    _extract_source_earliest_finance_date,
    _fetch_massive_finance_payload,
    _has_non_empty_value,
    _is_canonical_finance_payload,
    _is_core_finance_report,
    _is_empty_finance_payload,
    _is_raw_finance_payload,
    _is_reusable_finance_payload,
    _is_supported_finance_payload,
    _is_truthy,
    _is_valuation_finance_report,
    _json_dumps_compact,
    _log_finance_payload_observation,
    _normalize_report_name,
    _parse_iso_date,
    _payload_has_dates_on_or_after,
    _payload_report_dates,
    _payload_row_items,
    _safe_close_massive_client,
    _stable_finance_payload,
    _summarize_exception,
    _summarize_massive_payload,
    _truncate_trace_text,
    _validate_environment,
    _count_usable_payload_rows,
    fetch_and_save_raw,
)

_COMPAT_EXPORTS = (
    _ThreadLocalMassiveClientManager,
    _decode_payload_json,
    _emit_bounded_trace,
    _extract_latest_finance_report_date,
    _extract_source_earliest_finance_date,
    _fetch_massive_finance_payload,
    _has_non_empty_value,
    _is_canonical_finance_payload,
    _is_core_finance_report,
    _is_empty_finance_payload,
    _is_raw_finance_payload,
    _is_reusable_finance_payload,
    _is_supported_finance_payload,
    _is_truthy,
    _is_valuation_finance_report,
    _json_dumps_compact,
    _log_finance_payload_observation,
    _normalize_report_name,
    _parse_iso_date,
    _payload_has_dates_on_or_after,
    _payload_report_dates,
    _payload_row_items,
    _safe_close_massive_client,
    _stable_finance_payload,
    _summarize_exception,
    _summarize_massive_payload,
    _truncate_trace_text,
    _validate_environment,
    _count_usable_payload_rows,
    fetch_and_save_raw,
)
