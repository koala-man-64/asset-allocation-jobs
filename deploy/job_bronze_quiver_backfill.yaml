location: East US
name: bronze-quiver-backfill-job
type: Microsoft.App/jobs
tags:
  owner: ${RESOURCE_TAG_OWNER}
  cost-center: ${RESOURCE_TAG_COST_CENTER}
  workload: ${RESOURCE_TAG_WORKLOAD}
  environment: ${RESOURCE_TAG_ENVIRONMENT}
  job-category: data-pipeline
  job-key: quiver
  job-role: backfill
  trigger-owner: operator
identity:
  type: UserAssigned
  userAssignedIdentities:
    "${ACR_PULL_IDENTITY_RESOURCE_ID}": {}
properties:
  configuration:
    triggerType: Manual
    manualTriggerConfig:
      parallelism: 1
      replicaCompletionCount: 1
    replicaRetryLimit: 0
    replicaTimeout: 14400
    registries:
    - server: assetallocationacr.azurecr.io
      identity: "${ACR_PULL_IDENTITY_RESOURCE_ID}"
    secrets:
    - name: azure-storage-connection-string
      value: ${AZURE_STORAGE_CONNECTION_STRING}
    - name: pg-dsn
      value: ${POSTGRES_DSN}
  environmentId: ${CONTAINER_APPS_ENVIRONMENT_ID}
  template:
    serviceAccountName: ${SERVICE_ACCOUNT_NAME}
    containers:
    - env:
      - name: AZURE_FOLDER_QUIVER
        value: ${AZURE_FOLDER_QUIVER}
      - name: AZURE_CONTAINER_COMMON
        value: ${AZURE_CONTAINER_COMMON}
      - name: AZURE_CONTAINER_BRONZE
        value: ${AZURE_CONTAINER_BRONZE}
      - name: AZURE_CONTAINER_SILVER
        value: ${AZURE_CONTAINER_SILVER}
      - name: AZURE_CONTAINER_GOLD
        value: ${AZURE_CONTAINER_GOLD}
      - name: LOG_FORMAT
        value: JSON
      - name: LOG_LEVEL
        value: INFO
      - name: DISABLE_DOTENV
        value: "true"
      - name: SYSTEM_HEALTH_ARM_SUBSCRIPTION_ID
        value: ${AZURE_SUBSCRIPTION_ID}
      - name: SYSTEM_HEALTH_ARM_RESOURCE_GROUP
        value: ${RESOURCE_GROUP}
      - name: JOB_STARTUP_API_CONTAINER_APPS
        value: ${JOB_STARTUP_API_CONTAINER_APPS}
      - name: AZURE_CLIENT_ID
        value: ${ACR_PULL_IDENTITY_CLIENT_ID}
      - name: TRIGGER_NEXT_JOB_NAME
        value: ${SILVER_QUIVER_JOB}
      - name: AZURE_STORAGE_ACCOUNT_NAME
        value: ${AZURE_STORAGE_ACCOUNT_NAME}
      - name: AZURE_STORAGE_CONNECTION_STRING
        secretRef: azure-storage-connection-string
      - name: POSTGRES_DSN
        secretRef: pg-dsn
      - name: ASSET_ALLOCATION_API_BASE_URL
        value: ${ASSET_ALLOCATION_API_BASE_URL}
      - name: ASSET_ALLOCATION_API_SCOPE
        value: ${ASSET_ALLOCATION_API_SCOPE}
      - name: ASSET_ALLOCATION_API_TIMEOUT_SECONDS
        value: "120"
      - name: QUIVER_DATA_JOB_MODE
        value: historical_backfill
      - name: QUIVER_DATA_TICKER_BATCH_SIZE
        value: ${QUIVER_DATA_TICKER_BATCH_SIZE}
      - name: QUIVER_DATA_HISTORICAL_BATCH_SIZE
        value: ${QUIVER_DATA_HISTORICAL_BATCH_SIZE}
      - name: QUIVER_DATA_SYMBOL_LIMIT
        value: ${QUIVER_DATA_SYMBOL_LIMIT}
      - name: QUIVER_DATA_PAGE_SIZE
        value: ${QUIVER_DATA_PAGE_SIZE}
      - name: QUIVER_DATA_MAX_PAGES_PER_REQUEST
        value: ${QUIVER_DATA_MAX_PAGES_PER_REQUEST}
      - name: QUIVER_DATA_SEC13F_TODAY_ONLY
        value: ${QUIVER_DATA_SEC13F_TODAY_ONLY}
      image: ${JOB_IMAGE}
      imageType: ContainerImage
      name: bronze-quiver-backfill-job
      command: ["python", "-m", "tasks.quiver_data.bronze_quiver_data"]
      resources:
        cpu: 2.0
        memory: 4Gi
  workloadProfileName: Consumption
